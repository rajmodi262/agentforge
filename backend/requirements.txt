# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy==2.0.36
alembic==1.14.1
psycopg2-binary==2.9.10
asyncpg==0.30.0

# LangGraph (Agentic Orchestration)
langgraph>=1.1.0
langchain-core>=1.3.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Anthropic Claude API
anthropic==0.42.0

# Google Gemini API
google-genai>=1.74.0

# Groq API (fast LLM inference — Llama 3.3 etc.)
groq>=0.13.0

# Model Context Protocol (real stdio MCP server — app/mcp_stdio.py)
mcp[cli]>=1.2.0

# Pydantic
pydantic==2.10.4
pydantic-settings==2.7.1

# HTTP Client (for Brave Search)
httpx==0.28.1

# PDF Generation
reportlab==4.2.5

# Vector store / embeddings for RAG are OPTIONAL and heavy
# (sentence-transformers pulls in torch, ~2GB). The app runs fine without them —
# rag_service disables itself gracefully. To enable vector RAG, install:
#   pip install -r requirements-optional.txt

# Rate Limiting
slowapi==0.1.10

# Task Queue (Phase 5)
celery[redis]>=5.3.0
redis>=5.0.0

# Utilities
tenacity>=8.0.0
python-dotenv==1.0.1
websockets==14.1
email-validator==2.1.0
bleach==6.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
